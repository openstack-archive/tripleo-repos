============
Installation
============

At the command line::

    $ pip install tripleo-repos

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tripleo-repos
    $ pip install tripleo-repos
